
  # Design VersionDesk Dashboard UI

  This is a code bundle for Design VersionDesk Dashboard UI. The original project is available at https://www.figma.com/design/yta2rhwMMbz40VBDg0JWGO/Design-VersionDesk-Dashboard-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  